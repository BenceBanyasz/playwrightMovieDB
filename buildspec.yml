version: 0.2

phases:
    install:
        runtime-versions:
            nodejs: 18
    build:
        commands:
            - echo Build started on `date`
            - echo Building and running tests
            - docker build -t playwright-docker .
            - docker run -it --rm --ipc=host playwright-docker:latest npm run test
